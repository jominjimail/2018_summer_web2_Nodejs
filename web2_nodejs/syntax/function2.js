console.log(Math.round(1.6));
console.log(Math.round(1.2));


function sum (first , second){ //parameter
  return first+second;
}
console.log(sum(2,4)); //argument

Math.round(1.6); // 이건 콘솔에 출력이 되지 않는다. ㅇㅋ 이게 더 좋은 함수이다.
